You are annotating a chain-of-thought trace for cache insertion.

Task:
Given a question and a CoT trace segmented into numbered units, return the unit IDs after which [CACHE] should be inserted.

Context:
The trace reasons over a long input containing a few facts relevant to the question (like "Mary journeyed to the bathroom") scattered among many similar-shaped distractor facts about unrelated people or objects. The trace filters relevant from distractors and combines the relevant facts into an answer.

When to cache:
A unit qualifies if BOTH:
1. It contains a committed conclusion — a relevant fact, an intermediate state during tracking, or a resolved ordering.
2. The conclusion grounds the answer (directly, or via later units that build on it). Merely mentioning a fact — listing it, or dismissing it as irrelevant — does NOT count.

A trace typically contains several relevant facts and states worth caching; do not stop at one.

NEVER cache:
- The final unit, regardless of content.
- Distractor facts (most needle-shaped sentences in the trace are distractors).
- Strategy ("Let me re-read"), hedging ("maybe"), candidate hypotheses, or abandoned conclusions.
- Verification or restatements that re-state earlier content without adding new information.

Output:
Return one JSON object: {"cache_unit_ids": [<sorted ints>]}. Use [] if no units qualify. No prose, no markdown.

Examples:

Select [7] "Mary went to the bathroom." → relevant fact grounding the answer.
Select [15] "So the apple's locations were: garden → bathroom → kitchen." → consolidated state grounding the answer.
Skip [10] "Daniel went to the garden." → distractor, not used to derive the answer.
Skip [41] "Let me double-check — Daniel was in the bedroom, so the football is there." → verification.
Skip [24] "So Mary's location is bathroom." → final unit.

Now apply to the following. Return JSON only.

Question:
{problem}

Numbered units:
{units}

Answer:
